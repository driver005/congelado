load("//bazel:build_defs.bzl", "congelado_module_library")

# Generated by include/cc/abi_gen's one consolidated genrule (real Clang AST — see
# include/cc/abi_gen/README.md), not a genrule local to this package: one process there covers
# every pilot domain in one run, which is what makes its TypeRegistry completeness check actually
# mean something. The checked-in cache.cppm sitting alongside this BUILD file is left in place,
# unreferenced, for the duration of the pilot — an easy revert is just pointing
# primary_interface back at it.

# Pure cache-backend interface — abstract base class only. Zero dependency on any specific
# backend implementation; implementations depend downward on this target and register a factory
# function pointer into ice::sonic::Registration (cc_abi_sonic_registration) instead.
congelado_module_library(
    name = "cc_abi_builder_cache",
    primary_interface = "//include/cc/abi_gen:generated/builder/cache/cache.cppm",
    partitions = [
        ],
    deps = [
        "//include/cc/abi/sonic/intern:cc_abi_sonic_intern",
    
        "//include/cc/abi/primitives:cc_abi_primitives",
],
    visibility = ["//visibility:public"],
)
