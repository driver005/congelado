
            .{{ slot_name }} = [](void* plugin_context, TF_Status* {{ status_name }}) noexcept -> {{ raw_return_type }} {
                auto* self = {{ class_name }}::create(plugin_context);
                auto res = self->{{ slot_name }}();
                if (!res) {
                    res.error().to_c({{ status_name }});
                    return nullptr;
                }
                return res->get_handle();
            },
