module core.manager.plugin;
@nogc nothrow:

// Umbrella re-export — mirrors C++ `export import :handle; :loader; :handler;`
public import core.manager.handle;
public import core.manager.loader;
public import core.manager.handler;
