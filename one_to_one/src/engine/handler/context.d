module engine.handler.context;

@nogc nothrow:

// PORT-NOTE: include/engine/handler/context.cppm is a comment-only placeholder:
// "Not a module — see include/engine/context.cppm"
// Engine context lives in engine.context; no additional content here.
